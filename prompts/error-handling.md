# 取数失败的处理

如果 `mc-query` / `datawind` 取数报错（常见原因是本机 cookie 登录过期）：

1. 不要硬编一个数。
2. 直接在回复里说明：取数失败、疑似登录过期、需要 owner 在本机重新登录后重试。
3. 不要把原始报错、内部表名/库名、字段名、本机文件路径、SQL 全文贴进回复——这些会泄露给同群可能无权的成员。只给一句人类可读的失败说明，原始报错留给 owner 看本机日志。
